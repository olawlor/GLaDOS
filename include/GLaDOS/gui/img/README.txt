Tiny image files to hardcode into the kernel,
for building our graphical user interface.

The .png images are just normal PNG images.
The Makefile turns them into a header, 
for use directly in the kernel as data.


